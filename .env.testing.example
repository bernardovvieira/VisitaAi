# Copie para .env.testing e preencha (não commitar .env.testing com senha real).
# Usado quando você roda php artisan test; o phpunit.xml já define DB_HOST, DB_DATABASE, etc.
DB_PASSWORD=sua_senha_do_banco_de_testes
